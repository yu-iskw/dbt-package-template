[project]
name = "integration-tests"
version = "0.1.0"
description = "Integration tests for dbt-package-template"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pytz>=2015.7",
    "nox>=2024.10.9",
]

[dependency-groups]
dbt-fusion = []
dbt-core-1-10 = [
    "dbt-core>=1.10,<1.11",
    "dbt-postgres>=1.10,<1.11",
    "dbt-duckdb>=1.10,<1.11",
]
dbt-core-1-11 = [
    "dbt-core>=1.11,<1.12",
    "dbt-postgres>=1.10,<1.11",
    "dbt-duckdb>=1.10,<1.11",
]

[tool.uv]
conflicts = [
    [
        { group = "dbt-fusion" },
        { group = "dbt-core-1-10" },
    ],
    [
        { group = "dbt-fusion" },
        { group = "dbt-core-1-11" },
    ],
    [
        { group = "dbt-core-1-10" },
        { group = "dbt-core-1-11" },
    ],
]

[tool.setuptools]
# Prevent setuptools from trying to build dbt folders as Python packages
packages = []
